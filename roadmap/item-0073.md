---
id: item-0073
status: ratified
arcs: [type-cleanliness]
rank: "0100"
title: "Stand up tools/linttypeflow from the audit extractor blueprint"
goal: "a standing flow-level lint gate rejects typed-value leaks the type checker can see, landing green with an owned allowlist, starting with the near-zero-false-positive check classes"
acceptance_intent: "the gate lives beside the existing linters as its own module with a reasoned allowlist whose empty state means the tree is clean for the covered classes; local roundtrips and typed-value-returned-as-plain-string gate first; a report mode emits the machine-readable survey format the audits standardized on; wired into the aggregate check in cheap-before-expensive order"
links: [docs/retrospectives/2026-07-07-dual-type-safety-audit.md, tools/linttypeconv/analyzer.go]
execution_profile: { class: small, reasoning: medium }
---

Blueprint: the operator-retained audit extractor source (single main.go on go/packages and go/types); only source enters the repo, never the prebuilt binary. One check class per landing, per the linttypeconv precedent; the first two classes (local roundtrips, typed value returned as plain string) measured near-zero false positives in the audit data. Escalation classes are item-0084.
