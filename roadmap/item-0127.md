---
id: item-0127
status: proposed
arcs: [adr-051-sealing, sigstore-harness]
rank: "0150"
title: "Live region-SBOM coverage: a run step with a file output in the registry-deploy itest lane"
goal: "the live registry-deploy integration test exercises the D9 region path end to end: a run step produces a named file or directory output, the deploy names it as a region, and the pushed subject carries the region SBOM referrers with the dev.strike.artifact annotation alongside the whole-image pair"
acceptance_intent: "the itest lane gains a run step with a file or directory output packed into the pushed image and named as a deploy artifact region; the live run pushes 2+2N SBOM referrers with dev.strike.artifact annotations matching the lane-declared names; the sealed record carries the region diff_id digest and both SBOM digests; the referrer-set assertion covers the region documents; the shell-free container choice for producing the output is settled at authoring"
links: [docs/ADR-051-deploy-as-sealing-point.md, cmd/strike/deploy_registry_integration_test.go, item-0114]
execution_profile: { class: medium, reasoning: medium }
---

Drift-invariant planning notes go here.
