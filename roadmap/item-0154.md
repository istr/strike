---
id: item-0154
status: ratified
arcs: [cue-coverage, test-hygiene]
rank: "0090"
title: "Gate the lane snippets embedded in documentation against the schema"
goal: "every fenced lane snippet in the tree's operator documentation is parsed and validated against the lane schema by a gate, so a snippet that drifts from the contract fails a check rather than misleading a reader"
acceptance_intent: "the gate walks the fenced yaml blocks that claim to be lane fragments -- README.md, docs/, test/sigstore-local/README.md -- and validates each against the lane schema; two design points are the reason this is not a one-liner and both are settled before it is implemented: documentation examples deliberately carry a named placeholder for the trust anchor rather than a certificate, so the gate substitutes a known-good anchor before parsing, and a fenced yaml block is not automatically a lane fragment, so the criterion for which blocks are in scope is explicit rather than inferred; the check is wired into make check and its value is demonstrated against the drift it would have caught, the README snippet that carried name: for id: on both step and outputs and omitted three mandatory blocks while nothing noticed"
links: [ADR-004, ADR-056, tools/lintdoc, docs/FIXTURE-TRUST-ANCHOR.md]
execution_profile: { class: middle, reasoning: brief }
---

Discovered while landing item-0153. The README lane snippet had carried four
schema errors for months -- name: instead of id: on the step and on both
outputs, and no resolver, oidc or keyless block -- and nobody noticed, because
nothing in the tree reads those blocks. The ADR-056 documentation pass corrected
them by hand, which fixes the instance and not the class.

Two things make this more than a parse loop. Documentation examples carry a
named placeholder for the trust anchor by ratified decision, not a certificate:
a real anchor is 596 characters of noise in a fifteen-line snippet, and the
fixture anchor in particular stands over a private key RFC 9500 publishes, so a
copyable example would declare an anchor anyone can mint against. The gate
therefore substitutes before it parses. And a fenced yaml block in a markdown
file is not necessarily a lane fragment -- some are compose files, some are
frontmatter, some are deliberate counter-examples -- so the in-scope criterion
has to be stated rather than guessed.

Open question for whoever takes it: whether the gate lives in tools/lintdoc,
which already walks the whole tree and owns the placeholder convention through
the trust-anchor allowlist, or in a tool of its own. lintdoc has the walk and
the file-kind boundary already; against that, it currently needs no CUE and this
would give it one.
