---
id: item-0119
status: proposed
arcs: [test-hygiene]
rank: "0040"
title: "Fail-fast gate for the Cloudflare fingerprint integration tests"
goal: "the three Cloudflare fingerprint integration tests obey the DEVELOPMENT.md section 2.5 fail-fast contract: they run by default under the single STRIKE_INTEGRATION switch and fail with the opt-out hint instead of silently skipping when their trust anchor is unavailable"
acceptance_intent: "no integration test skips on an unset env variable other than STRIKE_INTEGRATION=0; the rotating Cloudflare fingerprint has a deterministic source or the tests are retargeted at the harness DoT resolver; a full-tree test run with the harness up executes all three"
links: [internal/transport/dial_integration_test.go, internal/transport/resolver_integration_test.go, internal/mediator/mediator_integration_test.go, docs/DEVELOPMENT.md, test/sigstore-local/coredns]
execution_profile: { class: medium, reasoning: brief }
---

Three tests skip silently when STRIKE_CLOUDFLARE_DOT_FINGERPRINT (and, for the mediator test, STRIKE_CLOUDFLARE_HTTPS_FINGERPRINT) is unset, while their own doc comments claim they fail fast -- the comment and the code contradict each other. Complication: the Cloudflare fingerprint rotates and has no harness-local source, so a plain Fatalf would make the suite fail on every fresh machine. The harness now carries a CoreDNS DoT resolver with a committed-key-free pinned certificate (pki/resolver.crt), which is a candidate replacement anchor; whether the public-Cloudflare coverage is kept alongside it is part of this item.
