---
id: item-0140
status: proposed
arcs: [adr-052-lock-free]
rank: "0400"
title: "Bind SSH bridges to a capsule-owned context, delete the client set, and finish the lock-free conversion with its gate"
goal: "the capsule owns a bridge context and join; CloseOutbound cancels and joins instead of sweeping a tracked client set, which is deleted; SSH bridge records flow through the capsule's single owner; the capsule lifecycle is an atomic monotone state machine; make lint gains the ADR-052 D7 gate requiring zero banned sync identifiers in production Go (ADR-052 D1, D2, D3, D6, D7)"
acceptance_intent: "production code under cmd/ and internal/ declares no banned sync primitive and the make lint gate observes it; a bridge in flight at step end is ended by cancellation within a bound and its record appears in Records after Stop; Start and Stop stay idempotent and mutually exclusive by compare-and-swap; make check is green under -race"
links: [internal/capsule/capsule.go, internal/capsule/sshforward.go, internal/front/front.go, cmd/strike/run.go, Makefile, ADR-052, ADR-038, item-0136]
execution_profile: { class: large, reasoning: deep }
---

Hard-sequenced after item-0136, which shares the capsule surface and
makes serve cancellation real; the bridge-dial context binding lands
here if that item has not absorbed it. The tracked client set exists
only to compensate for a dial that is not bound to its context, so
binding the dial deletes the set and the lazy-reinit race with it;
CloseOutbound becomes cancel-and-join on a capsule-owned bridge
context, which is also the join point the SSH records need. This item
carries the ADR-052 D7 gate because it is ranked last in the arc; if
the arc is reordered, the gate moves to whichever conversion item
lands last.
