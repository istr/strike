---
id: item-0056
status: ratified
arcs: [test-hygiene]
rank: "0030"
title: "Bind the crossval positive-case inputs to the real lane schemas"
goal: "the cross-validation vectors positive-case structured inputs are typed against the same lane schemas they cross-validate, so the vectors type-check against the contract instead of accepting any shape"
acceptance_intent: "the assemble-vector spec input is bound to the lane pack-spec type and the render-known-hosts peers input to the lane peer type; the attestation-vector input stays open because its negative cases are validated by the expected-valid flag; the vectors still validate and crossval tests pass"
links: [contract/crossval/crossval.cue, contract/lane/lane.cue, contract/lane/peer.cue, internal/executor/crossval_test.go]
execution_profile: { class: small, reasoning: medium }
---

The crossval loader already resolves CUE imports natively (item-0031 over the embedded module, which subsumed and retired item-0030), so crossval can import lane today and this item has no pending predecessor. Negative-case openness is intentional and stays.
