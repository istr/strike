---
id: item-0078
status: ratified
arcs: [type-cleanliness]
rank: "0180"
title: "Give the local image reference one owner"
goal: "the digest-pinned local reference is typed at construction, decomposed only by owning accessors, and addressed by typed keys, so the independent split reimplementations and the dotted-ref map keying disappear"
acceptance_intent: "wrap builders return the image-ref primitive and the tag builder takes the identifier like its siblings; the resolved-output handle interface returns the typed reference and manifest-digest derivation uses the owning accessor; engine image-reference parameters are typed; producer caches and the step-input handle map key on typed values instead of dotted or raw strings; the spec-hash keying doc drift and the always-empty source-hash parameter are resolved"
links: [docs/retrospectives/2026-07-07-dual-type-safety-audit.md, item-0052, internal/registry/cache.go, internal/output/handle.go]
execution_profile: { class: small, reasoning: medium }
---

Register rows R-03, R-04, R-07, R-43. Depends on item-0052 landing first (contract root). Fork H is ratified (option 1): the owned decomposition is Repo() string and Digest() (primitive.Digest, error), Digest splitting at the last @ (the anchored regex meaning) rather than the first as both current implementations do; ParseImageRef(string) is the single validating entry and NewImageRef is deleted (zero production callers, all eleven sites are test-only). Internally composed references are typed at WrapDigest. The four independent split reimplementations collapse onto the accessors. The accessors land here; the ParseImageRef entry policy and the executor-guard rewrite follow in item-0082. Recorded caveat: engine image-reference parameters (ImageInspect, ImageTag, SaveImage) accept both tag and digest forms, so "engine image-reference parameters are typed" must verify that domain before retyping to ImageRef, and the internal/container/parseImageRef helper splits a repo:tag reference, not an ImageRef, colliding conceptually with primitive.ParseImageRef.
