---
id: item-0067
status: proposed
arcs: [lane-dag-decomposition]
rank: "0020"
title: "Extract a separate lane validation phase from the DAG resolve methods"
goal: "Lane-structure validation is a distinct phase between parse and DAG build; the reference-integrity checks now inline in the DAG resolve methods and the scattered pure-lane validators live in one place and run over the lane and the index; only graph-topology validation stays with the DAG."
acceptance_intent: "A lane/validate.go hosts the reference-integrity checks (unknown step, output not found, subpath on file output, no image output, disjunction dispatch), the findOutput helper, and the pure-lane validators (output-id disjointness, provenance paths, peer anchors, base-sbom trust anchor, path validation); mount-disjointness runs via the lane input iterator; the orchestration runs parse then validate-lane as distinct steps; the DAG resolve methods no longer carry inline validation; the two leaf-topology validators remain DAG validators; make check is green and behavior is unchanged."
links: [internal/lane/parse.go, internal/lane/dag.go]
execution_profile: { class: medium, reasoning: high }
---

Depends on item-0066 (index and iterators). Separates the two concerns the resolve methods currently mix: reference-integrity validation moves to the lane phase; edge and adjacency building stays. Leaf-topology validation needs the reverse adjacency, so it belongs with the DAG, not this phase.
