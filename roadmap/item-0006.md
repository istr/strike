---
id: item-0006
status: ratified
arcs: [adr-040-keyless, sigstore-harness]
rank: "0010"
title: "ADR-040 2c: live end-to-end base-SBOM verification against the harness"
goal: "The base-SBOM verification path runs live end to end against the sigstore-local harness, covering what the hermetic fixture tests cannot"
acceptance_intent: "A live run against test/sigstore-local covers, together: (a) cosign-faithful empty-config referrer discoverability that the in-memory test registry cannot reproduce, and (b) the full fetch -> verify -> resolvedDependencies path end to end, from FetchBaseSBOMReferrers through verifyBaseSBOMs to the sealed SLSA provenance"
links: [ADR-040, ADR-019]
execution_profile: { class: small, reasoning: medium }
---

2c is otherwise LANDED (referrer fetch path, build guards, producer-side verification, hermetic fixture tests). This is the only residual: the live e2e. A reproducible path may want a small harness target (sign a throwaway base image via cosign attest against the harness signing-config); not built in the 2c spike, flagged as harness shell-corpus debt.
