---
id: item-0077
status: ratified
arcs: [type-cleanliness]
rank: "0170"
title: "Type provenance timestamps and route the target reference findings"
goal: "provenance fetch timestamps carry the timestamp primitive under a reconciled scalar doc, and the target url and type findings become decision inputs to the pending deploy-target namespace item instead of ad hoc fixes"
acceptance_intent: "all four provenance record variants type fetchedAt as primitive.#Timestamp and the scalar doc drops its attestation-facing exclusion; the target findings are recorded against the existing namespace decision item; no target schema change and no second timestamp scalar happen here"
links: [docs/retrospectives/2026-07-07-dual-type-safety-audit.md, contract/provenance/provenance.cue, contract/target/target.cue, item-0057]
execution_profile: { class: smallest, reasoning: none }
---

Register rows R-38 and R-39. Fork E is ratified (option 1): fetchedAt (all variants) types primitive.#Timestamp, and the scalar's doc comment drops the attestation-facing exclusion because it described an intended usage split, not a mechanism, and one RFC3339 grammar suffices for both informational and attestation-facing timestamps. scalars.cue doc comments are code comments, not ADR text; the reasoning is recorded here. No second #AttestTimestamp scalar is introduced. The target half is bookkeeping into item-0057, deliberately not a schema change here.
