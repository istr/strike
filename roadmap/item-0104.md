---
id: item-0104
status: ratified
arcs: [adr-051-sealing, type-cleanliness]
rank: "0040"
title: "Relocate the provenance declaration and single-home SourceType"
goal: "SourceType has a single home in the provenance package that lane consumes, and the ProvenanceSpec declaration sits with the step rather than among the deploy types, per ADR-051 D6"
acceptance_intent: "the lane package defines no SourceType of its own and imports provenance for it; ProvenanceSpec.type is provenance.SourceType; the ProvenanceSpec definition and the step provenance field are in the step region, not among the deploy types; the generated Go compiles without a duplicate import; make check is green"
links: [docs/ADR-051-deploy-as-sealing-point.md, contract/provenance/provenance.cue, contract/lane/lane.cue]
execution_profile: { class: small, reasoning: medium }
---

ADR-051 D6 relocation plus the SourceType single-home target. SourceType is duplicated today in provenance.cue and lane.cue; the canonical home is provenance.cue. ProvenanceSpec stays a lane/step concern, not moved into the provenance package. Delegate-measure the gengotypes cross-package enum reference in a throwaway worktree before predicting the @go form: lane imports struct types cross-package today (target.Deploy) but not a string disjunction.
