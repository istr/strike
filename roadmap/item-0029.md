---
id: item-0029
status: proposed
arcs: [spec-layering, parked]
rank: "0100"
title: "Constrain trust-root rawBytes with base64"
goal: "the trust-root rawBytes fields carry the base64 constraint via base-scalars #Base64 instead of a bare string"
acceptance_intent: "the two wire-trustroot rawBytes sites reference #Base64; because they are bare strings today this is a validation tightening rather than a wire-neutral home-move, so it lands as its own change; golden trust-root fixtures still validate unchanged"
links: [ADR-047, specs/wire-trustroot.cue, specs/base-scalars.cue]
execution_profile: { class: small, reasoning: brief }
---

Split out of item-0024: the rawBytes sites are bare strings, so pointing them at #Base64 adds a constraint instead of collapsing a duplicated regex. Depends on #Base64 living in base-scalars (item-0024).
