---
id: item-0002
status: ratified
arcs: [adr-038-ssh-front]
rank: "0010"
title: "ADR-038 item 9: SSH-mediated per-connection observed records"
goal: "The SSH mediator emits per-connection observed records (host-key fingerprint, negotiated algorithms, allowlisted command) into the capsule records, completing observed-peer coverage alongside the TLS and DoT mediators"
acceptance_intent: "Each mediated SSH connection produces a Layer-V observed record (upstream host-key fingerprint, negotiated algorithms, allowlisted command) fed into the capsule records that ingestRecords consumes; the engine-asserted attribution of connection-to-step (Layer E) is recorded; observed-peer coverage matches what the TLS and DoT mediators already emit"
links: [ADR-038, ADR-037]
execution_profile: { class: small, reasoning: medium }
---

The collection side already landed (collectObservedPeers/ingestRecords fold capsule-observed records into sealed.observed_peers (V) and engine_dependent.peer_attribution (E)). What remains is the SSH-mediated path emitting the per-connection records. Self-contained; parallelizable with the observed-TLS consolidation item. == cross-roadmap execution-order step 9.
