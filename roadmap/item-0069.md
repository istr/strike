---
id: item-0069
status: proposed
arcs: [lane-dag-decomposition]
rank: "0040"
title: "Add a runState-side image-from resolver for runtime-resolved producer image refs"
goal: "The image-from edge resolution lives on the run side as an allocation-light resolver that yields the producer's runtime-resolved image reference, not as a static lane resolver."
acceptance_intent: "A runState or runContext resolver surfaces the image-from producer image reference resolved at runtime (the value populated in runState.imageFromRefs after the producer step runs); the lane package holds no image-from lane resolver; the image-from consumers read the runtime resolver; the DAG ImageFromEdges map and resolveImageFromEdges may remain until a later decomposition item removes them; make check is green."
links: [internal/lane/dag.go, cmd/strike/run.go, item-0066]
execution_profile: { class: medium, reasoning: high }
---

Split out of item-0066. The image-from producer image reference is known only
after the producer step runs -- it is populated at runtime in
runState.imageFromRefs (cmd/strike/run.go) and consumed by resolveImageDigest.
A static lane resolver over the parsed lane and the step index therefore cannot
yield it, so image-from does not belong with the static inputs, pack-files, and
deploy-artifacts resolvers item-0066 adds. This item places the image-from
resolution on the run side. The design of that resolver -- a prebuilt map
guarded by a mutex versus a lazy runtime fill -- is itself unratified and is
resolved with the operator before an instruction is authored.
