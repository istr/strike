---
id: item-0141
status: proposed
arcs: [parked]
rank: "0140"
title: "Live kubernetes deploy integration test against a real cluster (parked post-beta)"
goal: "the kubernetes deploy is exercised end to end against a real API server in the local harness, not only against unit fakes"
acceptance_intent: "a harness-hosted cluster accepts an apply driven by the control plane; the sealed attestation carries the observed API-server identity; the test is skipped, not failed, when the harness is absent, matching the sigstore live-test convention"
depends_on: [item-0111]
links: [docs/ADR-054-kubernetes-deploy-via-cluster-api.md, test/sigstore-local/]
execution_profile: { class: large, reasoning: deep }
---

The registry side has this in item-0127; the kubernetes side has nothing equivalent, and unit fakes cannot show that a real API server accepts what the control plane sends. Needs a cluster in the local harness, which touches the same topology question item-0134 owns, so it is scheduled behind both that and the method itself.
