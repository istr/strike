---
id: item-0097
status: ratified
arcs: [build-toolchain, sigstore-harness]
rank: "0290"
title: "Retire the sigstore-local test Makefile so no Makefile remains in the tree"
goal: "the test/sigstore-local/Makefile is eliminated and its check-issuer gate is reachable go-natively, so no Makefile remains anywhere in the tree"
acceptance_intent: "test/sigstore-local/Makefile no longer exists; the issuer-consistency check it provided runs go-natively; the test/sigstore-local/README.md references are updated; a tree-wide find for Makefile returns nothing"
links: [test/sigstore-local/Makefile, test/sigstore-local/README.md]
execution_profile: { class: small, reasoning: low }
---

The second and last Makefile in the tree, under test/sigstore-local/. Its
check-issuer target verifies that two issuer strings agree; it belongs to the
sigstore test harness, hence the secondary sigstore-harness arc, but it is
listed under build-toolchain because "no Makefile remains" is a
build-toolchain-arc acceptance. Independent of the root-Makefile chain
(item-0096) -- it can land any time -- but the "tree-wide find for Makefile
returns nothing" claim is only true once both this and item-0096 have landed.
Lightweight; the issuer check is a small go-native comparison.
