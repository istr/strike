---
id: item-0091
status: ratified
arcs: [type-cleanliness]
rank: "0157"
title: "Decide the visibility of generated enum constants"
goal: "a package that consumes a discriminated union can name its discriminator values without spelling string literals"
acceptance_intent: "the genenums visibility policy is decided and applied: either the constants are exported, or the consuming packages reach the values through an accessor the owning package provides; the wire-discriminator literals in internal/container and internal/deploy are replaced by named symbols; the exhaustive linter covers the switch"
links: [docs/retrospectives/2026-07-07-dual-type-safety-audit.md, docs/TYPE-SURVEY-RULEBOOK.md, tools/genenums/main.go, internal/deploy/deploy.go, internal/container/podman.go]
execution_profile: { class: small, reasoning: medium }
---

Owner of the deferral ADR-049 records: genenums emits unexported constants (constIdent lowercases the type name), so hoisting a disjunction to a named definition types the field but leaves a consumer in another package without a symbol. Blocks the D1 remediation of the BC-03 sites outside the owning seam.
