---
id: item-0015
status: proposed
arcs: [adr-038-ssh-front]
rank: "0030"
title: "Deploy-path SSH enablement (lift the deploy-path SSH rejection)"
goal: "Deploy steps can use SSH peers through the control-plane front instead of being rejected: the deploy-path not-implemented guard is lifted, and deploy gets the same mediated SSH and per-step trust delivery the run path already has."
acceptance_intent: "A deploy step that declares an SSH peer is mediated through the front rather than rejected; per-step SSH trust material is delivered to deploy containers as it is for run steps; the deploy path emits the same observed (Layer V) and engine-asserted (Layer E) peer records the run path does; the change is gated behind the front rehosting that lands the additional protocols the deploy path requires."
links: [ADR-038, ADR-037]
execution_profile: { class: small, reasoning: medium }
---

Deferred follow-on noted under ADR-038 item 7 (run-path SSH trust delivery landed; the deploy path stays rejecting SSH peers with a not-implemented error until the front lands the protocols the deploy path needs). Not itemized in the legacy roadmap's implementation list -- surfaced during the roadmap-store coverage review. Gated on the front completion: depends on item-0003 (DoT/TLS rehost onto the front) and builds on item-0002 (SSH per-connection records) for deploy-side peer records. The run-path per-step SSH trust delivery already exists and is the pattern to extend to deploy. Unscheduled until the front items land; slot into _order.md after item-0003 then.
