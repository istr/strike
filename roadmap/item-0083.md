---
id: item-0083
status: ratified
arcs: [type-cleanliness]
rank: "0230"
title: "Eliminate in-band grammar sentinels"
goal: "no regex-constrained named type carries a zero or composite value its own grammar rejects; absence and the empty-output case get explicit representations"
acceptance_intent: "the empty digest sentinel, the empty-output reference key, and the empty trust-root casts are replaced by explicit optional or dedicated representations; the parse helpers stop round-tripping grammar-violating values by documented contract; this lands before any gate that enforces grammars at rest"
links: [docs/retrospectives/2026-07-07-dual-type-safety-audit.md, internal/lane/runtime.go, internal/primitive/digest.go]
execution_profile: { class: small, reasoning: medium }
---

Register row R-29. Prerequisite for any future gate that enforces scalar grammars at rest; interacts with fork A because the empty-output key is one of the sentinel carriers. Fork H (item-0082) deliberately concentrates the empty trust-root-ref sentinel at a single site (cmd/strike/verify.go guards on absence before ParseImageRef) precisely so this item finds it there; retiring ParseDigest's empty-round-trips-absence contract is this item's charter, which is why item-0082 does not build that contract into ParseImageRef.
