---
id: item-0008
status: ratified
arcs: [sigstore-harness]
rank: "0030"
title: "Harness H2: WebAuthn/FIDO2 identity hardening at the IdP"
goal: "Identity is hardware-gated at the IdP via WebAuthn passwordless, unblocking the real identity-gated producer path, with the backplane unchanged"
acceptance_intent: "Keycloak realm runs WebAuthn passwordless as a required authentication flow with user-verification required; README documents token registration and browser login; the identity-gated producer path works for both cosign and strike's in-process flow, each running as a container process against the host engine (ADR-055 D5); the FIDO2 token and the browser flow stay on the host side of the engine socket (Variant A), in the form the OIDC-flow measurement of the reference endpoint ratifies"
links: [ADR-040, docs/ADR-055-hardened-endpoint-development-premise.md]
execution_profile: { class: small, reasoning: medium }
---

Backplane (Keycloak/Fulcio/Rekor v2) stays containerized; browser + FIDO2 token + producer client run on the host. Whether the direct-access grant stays enabled afterward for CI convenience is an operator choice. Schedule when the hardware-gated identity path is needed. Depends on trusted-root-and-smoke (landed).

Amended 2026-09-05 at c01be87 against ADR-055. "Producer client run on the
host" contradicted D5: strike and cosign run as container processes
against the host engine. The browser is a different matter: D1 lists four
capabilities on the reference endpoint and a browser is not one of them.
How the OIDC login reaches the IdP from that endpoint is measured and
ratified before this item and item-0063 are authored (operator,
2026-09-05); this item's Variant A is restated once that measurement
exists.
