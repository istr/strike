---
id: item-0134
status: ratified
arcs: [sigstore-harness]
rank: "0300"
title: "Drive the sigstore harness from a declared CUE topology over libpod, without compose"
goal: "the harness topology is declared in CUE and realized by a Go driver over the libpod API, so compose.yaml and the external podman-compose provider are gone and bringing the harness up needs only podman"
acceptance_intent: "compose.yaml no longer exists; the bridge network, the three named volumes and the eight service containers are created from one declared topology with no external orchestration binary; the harness comes up on a machine that has podman and nothing else; readiness gates cover every service with a reachable probe endpoint rather than only the three the Makefile probed, the DoT resolver included; a config file edited in the working tree is visible to the container that consumes it without an operator-issued lifecycle command, so the interim stop-and-start step item-0119 carries is deleted with this item; the cosign conformance path either runs from a digest-pinned container through the engine API or is explicitly retained as a deliberate external-verifier exception"
depends_on: [item-0133]
links: [test/sigstore-local/compose.yaml, internal/container, contract, item-0007, item-0010, README.md, item-0119, test/sigstore-local/coredns]
execution_profile: { class: large, reasoning: high }
---

The end state of the harness arc, ratified as the target on 2026-08-20. The
README promises reproducible rootless lanes with no local toolchain. That
promise binds the product, and a lane user needs no compose -- but the harness
is the artifact with which a third party reproduces the attestation chain, and
today that needs more than podman. The sharper inconsistency is not the
toolchain but the pin: all nine default images in compose.yaml are digest
pinned, while the external compose provider that decides which containers exist
with which configuration is an unpinned distribution package. The optional
witness service is likewise built from a mutable git ref.

Shape: the topology is declared in CUE and a Go driver translates it into
libpod calls. Replacing a readable declarative file with several hundred lines
of imperative Go would trade one liability for a larger one; a driver that is a
translator rather than a topology keeps the declaration readable and satisfies
the CUE-first principle. The engine surface grows here -- network creation,
port publishing, network aliases, container creation -- and each addition is
separately ratified rather than arriving as a batch.

Owns the deferred full readiness coverage: the Makefile probed three of eight
default services, and the fourth, the timestamp authority, is covered for free
by the chain refresh. Extending gates to the remaining services is a behaviour
improvement rather than parity and belongs here rather than in the recovery
work.

Also owns a coupling worth solving here rather than separately: the harness
service addresses are hardcoded at seven sites across tests and the golden
generator, which binds the harness to a loopback host and breaks the moment the
engine holding it is not the local one. And the cosign conformance path, which
drives an external binary, can be brought under the no-subprocess invariant by
running cosign from a digest-pinned container through the engine API -- the same
machinery this item builds anyway -- without giving up the independent-verifier
property.

Two properties measured on 2026-08-22, while the integration tests were being
retargeted at the harness, belong here rather than in the recovery work.

The harness config files are mounted one file at a time, and a single-file bind
mount binds the inode rather than the directory entry. A write that replaces
the file instead of truncating it in place -- which is what the common editing
tools and a git checkout both do -- stays invisible to the running container
indefinitely, with no log line anywhere, because the consuming service sees an
unchanged file rather than a failed reload. The DoT resolver is where this
surfaced, but the mechanism is a property of the mount and not of that service:
six further config files are mounted the same way, the exported Caddy root that
Fulcio trusts among them, so a stale trust anchor is inside the blast radius. A
driver that realizes the topology chooses the mount shape and can make the
whole class impossible.

The recovery path waits on the issuer, the transparency log and the CT log, but
not on the resolver, even though a resolver probe is the first act of every
lane run and the production transport package already carries that probe. Until
both properties hold, item-0119 carries an explicit stop-and-start of the
resolver container as an interim step, and deleting it is part of this item's
acceptance.
