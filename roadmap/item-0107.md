---
id: item-0107
status: proposed
arcs: [adr-051-sealing, type-cleanliness]
rank: "0070"
title: "Restructure deploy artifacts into one pushed image plus SBOM regions"
goal: "the deploy artifacts schema is restructured so the pushed image and the SBOM regions are distinct by construction: one step-image subject plus a map of named output regions, per ADR-051 D9"
acceptance_intent: "DeploySpec.artifacts carries exactly one step-image reference, the pushed image and top-level subject, alongside a separate identifier-keyed map of output-region references; the one-image cardinality is structural, not validated at runtime; the region-map key is identifier-typed; the lane iterator no longer constructs the identifier from a string key and the run seam no longer projects it back to a string; no named-type conversion of a region name survives; the attest Sealed side mirrors the split; make check is green"
links: [item-0100, docs/ADR-051-deploy-as-sealing-point.md, contract/lane/lane.cue, contract/attest/attestation.cue]
execution_profile: { class: medium, reasoning: high }
---

ADR-051 D9. Restructuring chosen over a map constraint (ratified cut-fork): it makes the D9 cardinality structural and absorbs the R-05 key-typing and wrap-unwrap churn item-0100 tracked, so item-0100 is retired moot alongside this item. The #ArtifactSource disjunction and its default-marker subtlety dissolve. Blast radius: resolveArtifactDigests, validate.go, dag.go, iterator.go, run.go, projectSubject, and the attest Sealed.artifacts map plus its goldens. The D9 amendment must land in the ADR first.
