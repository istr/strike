---
id: item-0111
status: ratified
arcs: [adr-051-sealing]
rank: "0110"
title: "Source kubernetes manifests from artifacts, not strike stdin"
goal: "the kubernetes deploy sources its manifests from artifacts and no longer from strike's stdin, per ADR-051 D5"
acceptance_intent: "the kubernetes deploy reads its manifests from the resolved artifacts; no deploy path pipes strike's stdin into kubectl; the kubernetes artifacts cardinality D9 left open is settled here; make check is green"
links: [docs/ADR-051-deploy-as-sealing-point.md, internal/deploy/deploy.go]
execution_profile: { class: medium, reasoning: medium }
---

ADR-051 D5. Rewires deploy.go's opts.Stdin = os.Stdin manifest source to artifacts, the in-V condition for kubernetes. Depends on the artifacts restructure. D9 stayed silent on the kubernetes cardinality and what plays the role of the pushed image there; that is decided in this item.

Absorbed from the item-0114 completion report: the ratified D9 registry rules
made a registry deploy without artifacts invalid, so
internal/lane/testdata/valid_minimal.yaml grew a build step and an artifacts
map and no longer exercises a minimal lane. Once this item settles the
kubernetes artifacts semantics, restore valid_minimal.yaml as a genuinely
minimal one-step kubernetes-method lane (kubernetes deploys need only entry
integrity), so the fixture is reshaped once, not twice.
