---
id: item-0030
status: ratified
arcs: [test-hygiene]
rank: "0020"
title: "Consolidate meta-crossval sha256 and rework crossval test schema loading"
goal: "meta-crossval references base-scalars #Digest instead of a local copy, with crossval_test.go loading the schema module-aware"
acceptance_intent: "crossval_test.go resolves CUE module imports so meta-crossval can import lane; the interim local #Digest in meta-crossval is replaced by lane.#Digest; vectors still validate"
links: [ADR-047, internal/executor/crossval_test.go, specs/meta-crossval.cue, specs/base-scalars.cue]
execution_profile: { class: small, reasoning: medium }
---

Split out of item-0024: meta-crossval is package crossval compiled standalone via ctx.CompileBytes in crossval_test.go, which cannot resolve module imports (see internal/deploy/validate.go). Cross-package reference to base-scalars #Digest needs that load path reworked. Interim in item-0024: meta-crossval gets a local #Digest so its inline sha256 regexes are deduplicated within the file.
