---
id: item-0004
status: ratified
arcs: [engine-cluster]
rank: "0010"
title: "Observed-TLS identity consolidation (engine-cluster lead)"
goal: "The observed TLS server identity has a settled shape across peers, resolver, and engine -- either one shared observed-TLS-server-identity type in transport.cue or a deliberate role-distinct decision -- so the engine-hardening matching is written once against it"
acceptance_intent: "The three diverging shapes (#ObservedTLS for peers; #ResolverRecord for the resolver; #EngineServerTLS in the engine union) are reconciled by an operator-ratified decision: factor one shared observed-TLS-server-identity type per the single-sourcing principle, or keep them role-distinct with the rationale recorded; the chosen shape is settled before the hardening matching is authored; as part of the same consolidation the certificate-fingerprint fields stop being bare strings and carry the digest or bare-sha256 primitive, and the prefixed-sha256 versus bare-hex format split across the engine, peer, and resolver fingerprints is unified to one form so cross-comparison holds; resolver-sealed change regenerates goldens"
links: [ADR-038, ADR-037, contract/primitive/scalars.cue]
execution_profile: { class: small, reasoning: high }
---

Engine-cluster LEAD: settles the shape so the hardening matching (engine-hardening item) is written once. Golden-affecting (resolver sealed). == cross-roadmap execution-order step 8. Migrated from the retired cue-spec-review.
