---
id: item-0102
status: ratified
arcs: [adr-051-sealing]
rank: "0020"
title: "Remove PackSpec.configFiles and FileEntry"
goal: "PackSpec.configFiles and the FileEntry type are removed so the pack payload comes only from digest-pinned inputs, per ADR-051 D8"
acceptance_intent: "PackSpec.configFiles and FileEntry no longer exist in the lane schema; no pack consumer reads config-file layers; the config-file layering path and its integration fixtures are gone; make check is green"
links: [docs/ADR-051-deploy-as-sealing-point.md, contract/lane/lane.cue, internal/executor/pack.go]
execution_profile: { class: small, reasoning: low }
---

ADR-051 D8. Pack-side, disjoint from the deploy region, lands in any order. Consumers: addConfigFileLayers in pack.go and two sbom_test fixtures. Metadata-valued pack fields (config, annotations, per-file mode/uid/gid) stay per D8's payload line. The D8 amendment must be landed in the ADR first.
