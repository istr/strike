---
id: item-0110
status: ratified
arcs: [adr-051-sealing]
rank: "0100"
title: "Push the deploy image from the control plane, remove the engine push"
goal: "the control plane pushes the deploy image with remote.Write, and the engine push path and the lane-wide registry field are removed, per ADR-051 D4 and D6"
acceptance_intent: "lane.registry does not exist; no deploy pushes through the engine ImagePush path; the registry deploy subject is the resolved artifacts image and not a string source; DeployRegistry.source does not exist; the control plane writes the image with remote.Write against the declared trust anchor; a redirect whose final dialed host violates the trust anchor fails, and only the final dialed host after all redirects is attested; make check is green"
links: [docs/ADR-051-deploy-as-sealing-point.md, cmd/strike/run.go, internal/registry/client.go, internal/deploy/deploy.go]
execution_profile: { class: large, reasoning: high }
---

ADR-051 D4 and D6. remote.Write already exists for referrers (referrer.go); this routes the image push through the control plane and drops engine ImagePush for deploy. Consumers: run.go registry.Tag and pushAndReport, client.go, the deploy.go registry case. The redirect-trust-anchor rule is a runtime acceptance property, not schema. Sequenced after the target-grammar item (the destination remote.Write writes to) and the artifacts restructure (the subject it pushes).
