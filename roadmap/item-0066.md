---
id: item-0066
status: ratified
arcs: [lane-dag-decomposition]
rank: "0010"
title: "Move the step-id index to a Parse companion and add allocation-light lane iterators"
goal: "Parse returns the step-id to Step index as a companion value; lane exposes allocation-light iterator resolvers over the lane and that index, so per-step resolved views come from the lane, not from stored DAG edge maps."
acceptance_intent: "Parse returns the step index alongside the Lane and digest, and the duplicate-step-name check moves to index construction; lane provides iter.Seq resolvers for image-from, inputs, pack files, and deploy artifacts that resolve on-call from the lane and the index, returning the producer pair as an OutputRef, plus a companion resolver exposing the resolved producer objects where a consumer needs them; the DAG takes the index as a private reference handed over by the orchestration layer and the public DAG.Steps field is gone; the resolvers are additive with no edge map or consumer removed yet; make check is green."
links: [internal/lane/parse.go, internal/lane/dag.go, cmd/strike/run.go]
execution_profile: { class: medium, reasoning: high }
---

Foundational for the lane-validation and DAG-reduction items. The step index is a lane lookup structure, not DAG state, and cannot be a field on the CUE-generated Lane; it is carried as a Parse companion. The iterators replace the stored resolved edge maps as the source of per-step views.
