---
id: item-0052
status: ratified
arcs: [cue-coverage]
rank: "0020"
title: "Type the output handle image refs with the existing primitives"
goal: "output imageRef and layerDiffID carry the primitive types that already model their grammar, so the resolved-output handles are typed as strictly as every other image-ref field in the contract"
acceptance_intent: "output imageRef is typed as the image-ref primitive in repo-at-sha256 form and layerDiffID as the digest primitive, matching how lane types all of its image-ref fields; the file-handle struct is internally consistent, since its output identifier already uses the identifier primitive; generated types compile, make check is green, and goldens stay byte-identical because the named primitive marshals identically to the bare string"
links: [contract/output/output.cue, contract/primitive/scalars.cue, docs/ADR-046-one-canonical-digest-pinned-image.md, docs/retrospectives/2026-07-07-dual-type-safety-audit.md]
execution_profile: { class: small, reasoning: medium }
---

output is already CUE-first: it is in the gengotypes list and generates internal/output, so this item only tightens the field types within the existing CUE and migrates nothing. The hand-written internal/output handle file is the disjunction-glue interface and is out of scope. This is the standalone typing residue of the contract review B4 finding, decoupled from any output removal (the orphan reading was wrong; output is live via codegen).

Retrospective adjustment (dual type-safety audit): register row R-03. Items
0078 and 0079 depend on this typing landing first, and both audit lanes
enumerate its downstream consumer set.

Absorbed from the item-0114 completion report: selectImageArm, pushImageSBOM,
regionSBOM, and pushSBOMPair in internal/deploy/push.go bind a local or
parameter named name, shadowing the imported go-containerregistry pkg/name.
This item's consumer sweep edits exactly those functions (the fh.Ref and
fh.LayerDiffID call sites); rename the shadowing identifiers there so the
retyping cannot collide with a future name.NewDigest call.

Amended 2026-08-25 at dc83c97: the item-0079 half of the retrospective
adjustment above does not hold -- R-03's consumer set (C4-1, C1-4; E 100, 148,
97, 25, 26, 161, 157) is confined to the output contract fields, the generated
output types, the handle interface and their in-process readers, and shares no
site with item-0079's path rows (R-19, R-23, R-37, R-41); only item-0078
depends on this typing landing first.
