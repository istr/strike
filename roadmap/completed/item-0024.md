---
id: item-0024
status: done
arcs: [spec-layering]
rank: "0080"
title: "Consolidate duplicated value constraints into base-scalars"
goal: "the sha256, base64, git-commit and int64-string constraints each have one home in base-scalars, referenced everywhere else"
acceptance_intent: "each duplicated regex collapses to a single base definition referenced by qualified name; every move is wire-neutral and golden shows no diff; each consolidation is its own small change"
links: [ADR-047, docs/SPEC-PACKAGE-LAYERING.md, specs/lane.cue, specs/predicate.cue, specs/sigstore-bundle.cue, specs/source-provenance.cue]
execution_profile: { class: small, reasoning: brief }
---

Drift-invariant planning notes go here.

## Final summary

Consolidated all duplicated value constraints (#Base64, #GitCommit, #Sha256) into base-scalars.cue with @go(-) suppression. Replaced inline patterns in base-peer, base-provenance, base-transport, attest-predicate, and meta-crossval. meta-crossval now imports lane natively. Wire-neutral: generated types and golden fixtures unchanged.
