---
id: item-0029
status: done
arcs: [spec-layering, parked]
rank: "0100"
title: "Constrain trust-root rawBytes with base64"
goal: "the trust-root rawBytes fields in contract/lane/trustroot.cue carry the base64 constraint via primitive #Base64 instead of bare string"
acceptance_intent: "the three wire-trustroot rawBytes sites (publicKey.rawBytes, logId.keyId, certChain.certificates[].rawBytes) reference primitive.#Base64; because they are bare strings today this is a validation tightening rather than a type relocation, so it lands as its own change; golden trust-root fixtures still validate unchanged"
links: [ADR-047, contract/primitive/scalars.cue, contract/lane/trustroot.cue]
execution_profile: { class: small, reasoning: brief }
---

Split out of item-0024: the rawBytes sites are bare strings, so pointing them at #Base64 adds a constraint instead of collapsing a duplicated regex. Depends on #Base64 living in base-scalars (item-0024).

## Final summary

Added primitive.#Base64 constraint to three rawBytes sites in contract/lane/trustroot.cue (publicKey, logId, certChain). Wire-neutral; golden fixtures already contain valid base64.
