---
id: item-0076
status: done
arcs: [type-cleanliness]
rank: "0160"
title: "lane contract typing cluster: keys, references, defaults"
goal: "the lane contract stops declaring the secrets map key, the secret reference name, and the lane default timeout as bare strings where the typed form is proven expressible in this repository"
acceptance_intent: "the secrets map generates identifier-typed Go keys via the annotation form the attest package proves; the secret reference name is identifier-typed; the lane default timeout generates the duration type despite the literal default"
links: [docs/retrospectives/2026-07-07-dual-type-safety-audit.md, contract/lane/lane.cue]
execution_profile: { class: small, reasoning: medium }
---

Register rows R-05, R-06, R-18, R-19 (contract side), R-22, R-44. The typed-key annotation form is proven by the attest package maps. The #OIDCConfig and #SBOMSigner issuer and identity fields moved out to item-0085 (fork C): their ratified typing grew into one atomic wire-neutral change spanning endpoint, attest, and verify that cannot be split across this cluster and item-0081 without a half-typed intermediate. Every CUE change in here is individually confirmation-gated per AGENTS.md.

R-33 is delivered and dropped from this item. Two changes landed it ahead of
this cluster: the lane parse seam now decodes the schema-filled JSON, so every
non-optional CUE default reaches the Go struct instead of a zero value (commit
87c3b29), which removed the hand-written strategy fallback; and the strategy
disjunction was hoisted to a top-level definition, so it generates a named type
whose values are consumed through an exhaustive switch (commit aa1a9a1). The
lane default timeout row stays open: its parent block is optional, so the seam
does not fill it.

## Final summary

Three of the six original acceptance clauses landed at e0e8afbc7b: the secrets map key and the secret reference name are identifier-typed, and the lane default timeout generates the duration type despite its literal default, with the redundant round trip dropped from the run seam. The goal and acceptance_intent above were reduced to those three before this retire, because the other three were rerouted rather than delivered. R-05, the artifacts map key and the wrap-unwrap churn on the deploy-artifact path, moved to item-0100 and is blocked on the ADR-051 D6 enforcement shape. R-19, configFiles keys carrying the absolute-path primitive, is moot: ADR-051 D8 removes PackSpec.configFiles because lane literal text has no digest-pinned origin, so the key it would have typed ceases to exist. R-22, the registry constraint as a named primitive instead of an inline regex, is moot: ADR-051 D4 removes the lane-wide registry field.
