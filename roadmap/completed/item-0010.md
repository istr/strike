---
id: item-0010
status: done
arcs: [ct-conformance, sigstore-harness]
rank: "0020"
title: "Harness H3-3c: flag-clean cosign conformance target"
goal: "A cosign conformance target verifies the golden bundles offline with no --insecure-ignore-sct, gating exit on the V (sealed) layer only"
acceptance_intent: "The conformance Makefile target runs cosign offline-verify against the golden bundles flag-clean (no --insecure-* flags), relying on the embedded SCT verifying against the CT log in the trusted root; exit gates on the V (sealed) layer only"
links: [ADR-040]
execution_profile: { class: small, reasoning: medium }
---

Depends on H3-3b (the goldens must carry the ctlogs entry first). The minimal cosign v3.1.1 offline-verify invocation is measured and recorded; feeds this target.

## Final summary

Implemented by 386dba2 (2026-06-16), 'feat(harness): add flag-clean cosign conformance target', landed the same day as the item-0009 commit and likewise predating the item store; closed retroactively after verification on 2026-08-19. Verified: the conformance target at test/sigstore-local/Makefile verifies all three golden bundles offline with cosign v3.1.1 and carries no --insecure-* flag (the only 'insecure' match in the file is the echo banner). SCT enforcement was falsification-tested rather than assumed -- the sealed leaf carries a CT Precertificate SCT whose Log ID base64 equals the ctlogs keyId, and stripping ctlogs from a copy of the trusted root makes cosign fail with 'only able to verify 0 SCT entries; unable to meet threshold of 1', so the pass is load-bearing. Exit gates on the sealed (V) layer only: a sealed failure runs exit 1, while the engine-context and informational loop only echoes WARN and cannot alter the recipe exit status. The minimal-invocation measurement the body calls for is recorded in the commit message (--new-bundle-format dropped as deprecated and auto-detected in v3.1.1; --check-claims=false standing in for the synthetic golden subject). Two carried-forward notes: --check-claims=false means the gate does not bind the statement subject to an artifact, which is item-0007's live verify-attestation property; and the acceptance names a Makefile target that item-0097 will delete, so it needs repointing when that lands.
