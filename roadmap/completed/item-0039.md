---
id: item-0039
status: done
arcs: [spec-layering]
rank: "0150"
title: "Collapse DigestRef and the wire Digest into a single digest concept"
goal: "the content-addressed digest is one representation-neutral concept digest.Digest, with parse and render outward of the value, replacing the split wire-string Digest and structured DigestRef per ADR-048; supersedes the digest split introduced by item-0032"
acceptance_intent: "the content-addressed digest collapses onto the existing primitive.Digest wire type: the structured DigestRef type and the internal/lane digest bridge (ParseDigest/MustParseDigest/Wire/String/IsZero) are removed, contract/lane/digest.cue is deleted, and all callers reference primitive.Digest directly; the hand-written digest helpers are a slim boundary validator primitive.ParseDigest plus the inverse constructor/projection pair primitive.DigestFromHex and primitive.Digest.Hex(), with primitive.Digest.String() as the to-string chokepoint, living beside the existing primitive path helpers; the sha256 wire grammar remains primitive.#Digest, validated at the engine/registry boundary and by CUE at the serialization boundary; no new package and no concept-tier change (digest stays in primitive); ADR-046 gains an append-only refinement note; make check is green and golden fixtures are unchanged"
links: [ADR-048, ADR-046, internal/lane/digest_type.go, contract/lane/digest.cue, internal/primitive, item-0032]
execution_profile: { class: large, reasoning: deep }
---

Drift-invariant planning notes go here.

## Final summary

DigestRef and the internal/lane digest bridge removed; the content-addressed digest is now the single primitive.Digest wire value end to end. internal/primitive/digest.go holds ParseDigest (fail-closed boundary validator), DigestFromHex (the sha256:-prefix constructor, inverse of Hex and the single construction chokepoint), Digest.Hex() (bare-hex projection), and Digest.String(). contract/lane/digest.cue deleted; no new package and no concept-tier change. ADR-046 amended (append-only). Wire form byte-identical -- golden fixtures unchanged. make check green.
