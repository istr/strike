---
id: item-0009
status: done
arcs: [ct-conformance, sigstore-harness]
rank: "0010"
title: "Harness H3-3b: add ctlogs entry to goldenTrustedRoot and regenerate goldens"
goal: "goldenTrustedRoot carries the ctlogs entry (RFC6962 log id) and the golden bundles are regenerated against the CT-enabled harness"
acceptance_intent: "The ctlogs entry (RFC6962 log id = sha256(DER SubjectPublicKeyInfo), distinct from the Rekor v2 C2SP signed-note key id) is added to goldenTrustedRoot with the Rekor entry unchanged; the four goldens regenerate against the CT-enabled harness and pass make test fully offline"
links: [ADR-040]
execution_profile: { class: small, reasoning: medium }
---

Harness CT enablement (3a: TesseraCT service, Fulcio fileca + ct-log-url, make ctlog-pubkey) has landed; the spike returned GO. Golden regen is non-hermetic and non-deterministic (fresh Fulcio/Rekor/TSA material) -- expected. Cold-started harness re-keys trusted_root.json. Depends on the landed CT harness enablement.

## Final summary

Implemented by 7546fca (2026-06-16), 'feat(verify): seal the CT log into the golden trusted root', which predates the item store -- the item was transcribed from docs/ROADMAP-sigstore-test-harness.md without noticing the work had landed, and is closed retroactively after verification on 2026-08-19. Verified: the golden ctlogs entry carries logId.keyId = base64(sha256(DER SubjectPublicKeyInfo)) of its embedded key, recomputed independently, and that key is byte-identical to the harness pki/ctfe-pub.pem SPKI (PKIX_ECDSA_P256_SHA_256, SHA2_256). The Rekor tlogs logId uses the C2SP signed-note derivation and is not sha256(SPKI), so the two log ids are distinct as required. 7546fca regenerated exactly the four goldens (sealed, engine-context, informational, trusted_root). The golden-consuming tests pass with no external network, confirmed by running internal/verify and the cmd/strike verify tests inside an isolated network namespace. Caveat on the 'Rekor entry unchanged' clause: the Rekor key and Fulcio chain did change in that commit because the harness was cold-started between generations, which the item body anticipates; the clause holds in its intended sense, that the Rekor derivation was left untouched while ctlogs was added. Corroboration: ctlogs survived four later golden regenerations (e7d2efd, f872d43, 1da3a0b, 4ef9b63), still matches the live harness key, and cosign verifies the embedded SCT against it with no insecure flag.
