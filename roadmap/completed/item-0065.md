---
id: item-0065
status: done
arcs: [cue-coverage]
rank: "0020"
title: "Resolve lane.DAG mixing exported data with unexported derived state"
goal: "lane.DAG no longer trips the coverage linter's mixed-field rule: it stops holding exported data fields and unexported derived state in one struct, while its derived adjacency maps and graph methods are preserved and its cross-package consumers keep building"
acceptance_intent: "the coverage linter reports no mixed-field finding for lane.DAG; the derived adjacency state and the DAG graph methods still exist and behave identically; cmd/strike and the tests that read DAG fields still build; the remedy (all-unexported with accessors, or splitting the exported data from the unexported state) is the operator ratified choice, not fixed by this item"
links: [internal/lane/dag.go, item-0058, item-0050]
---

Surfaced by the coverage linter (item-0058): lane.DAG holds exported data
fields together with unexported derived adjacency state, so no single generated
definition can produce it. The sibling mixed struct transport.VerifiedConn is
handled separately. Two remedies are on the table -- make the struct
all-unexported with accessors, or split the exported data from the derived
state; the latter aligns with DAG's cue-coverage migration under item-0050. The
choice is unratified. Priority places this after the attestation-schema
migration (item-0050), though it may be taken tactically earlier.

## Final summary

Superseded by lane-dag-decomposition arc (items 0066-0068)
