---
id: item-0100
status: done
arcs: [adr-051-sealing, type-cleanliness]
rank: "0010"
title: "Type the deploy artifacts map key and retire the wrap-unwrap churn"
goal: "the deploy artifacts map generates identifier-typed Go keys and the artifact name stops being wrapped into the identifier by the lane iterator only to be projected back to a string at the run seam"
acceptance_intent: "the deploy artifacts map generates identifier-typed Go keys via the annotation form the attest package maps and the lane secrets map already prove; the lane iterator no longer constructs the identifier from a string map key; the deployer takes the artifact name in its typed form so the run seam stops projecting it back to a string; no named-type conversion of the artifact name survives on that path"
links: [docs/retrospectives/2026-07-07-dual-type-safety-audit.md, docs/ADR-051-deploy-as-sealing-point.md, contract/lane/lane.cue, item-0076]
execution_profile: { class: small, reasoning: medium }
---

Register row R-05, rerouted out of item-0076 when ADR-051 overtook the deploy region of the lane contract. The artifacts map stays: ADR-051 D9 records it as the SBOM decomposition of the one deployed image, so the identifier-typed key names an SBOM region and is a live retype, not moot. Gated on the artifacts schema change that enforces the D9 registry cardinality (exactly one step-image entry, the pushed image; zero or more output-arm entries, the SBOM regions), because R-05 retypes the key of that same map and the two edits touch adjacent lines. Do not author against this item until that schema shape is ratified. The typed-key annotation form is proven by the attest package maps and by the lane secrets map, which landed under item-0076.

## Final summary

Superseded moot by item-0107. The ratified cut for the artifacts question restructures DeploySpec.artifacts into one step-image subject plus a map of output regions (ADR-051 D9), which makes the one-image cardinality structural and carries the R-05 identifier-typed key and the wrap-unwrap churn removal inside item-0107. There is no separate map key to type once the map is the region map of that restructure, so this item has no remaining independent work.
