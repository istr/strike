---
id: item-0118
status: done
arcs: [adr-051-sealing, spec-layering]
rank: "0160"
title: "Drop the removed target package from the contract-layering docs"
goal: "no prose document still presents contract/target as a live concept-tier package: the package tree table, the concept-tier enumerations, the destuttering example, and the gengotypes and generated-file lists in AGENTS.md, docs/SPEC-PACKAGE-LAYERING.md, and docs/DEVELOPMENT.md match the tree that item-0113 left behind"
acceptance_intent: "a reader following AGENTS.md or docs/SPEC-PACKAGE-LAYERING.md to enumerate the contract packages or the generated Go files arrives at the set that actually exists; the destuttering example in SPEC-PACKAGE-LAYERING cites a surviving concept type instead of target.#Deploy; ADR-047, ADR-048, ADR-051 and the retrospectives are untouched as append-only history, and the unrelated '@go() package target' wording in ADR-047 and SPEC-PACKAGE-LAYERING is left alone; make check green"
links: [docs/ADR-051-deploy-as-sealing-point.md, docs/SPEC-PACKAGE-LAYERING.md, docs/DEVELOPMENT.md, AGENTS.md, item-0113]
execution_profile: { class: smallest, reasoning: none }
---

Documentation drift left by e7d2efd (item-0113, ADR-051 D10), which deleted contract/target without updating the prose that enumerates the concept tier. Three documents carry it, all as list or table entries: AGENTS.md names target in the concept-tier sentence and in the internal/{...}/*.gen.go list; SPEC-PACKAGE-LAYERING.md carries a target/ row in the package tree, names target in the concept-tier bullet, uses target.#Deploy as one of the destuttering examples, and lists target among the packages gengotypes runs over; DEVELOPMENT.md lists internal/target/target.gen.go among the generated files. The destuttering example needs a replacement citation, not just a deletion, since it illustrates a rule that still holds. Out of scope by the append-only rule: ADR-048 and ADR-051, which describe the type as it stood when they were written, and the 2026-07-07 dual-type-safety retrospective. A naive tree-wide grep for the word target is not a usable gate: ADR-047 and SPEC-PACKAGE-LAYERING both use target in the unrelated sense of an @go() annotation target, and AGENTS.md uses it in the grep-guidance prose.

## Final summary

Landed at 23560cc: dropped contract/target from AGENTS.md, docs/SPEC-PACKAGE-LAYERING.md, docs/DEVELOPMENT.md, contract/README.md, docs/CUE-WORKFLOW.md, CONTRIBUTING.md; corrected the adjacent gengotypes/attest generation story and replaced all make specs references with make generate. make check green.
