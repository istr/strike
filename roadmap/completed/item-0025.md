---
id: item-0025
status: done
arcs: [spec-layering]
rank: "0090"
title: "Update embed.go, Makefile, README and AGENTS.md for the new spec layout"
goal: "the build wiring and the docs describe the new prefixed-file, attest-package layout"
acceptance_intent: "embed.go and the make generate and specs selectors target the new file set; specs/README.md and the AGENTS.md schema-files and package-structure blocks describe the new layout; this runs last, when the tree already matches it; build and lint pass"
links: [ADR-047, docs/SPEC-PACKAGE-LAYERING.md, specs/embed.go, specs/README.md, AGENTS.md, Makefile]
execution_profile: { class: small, reasoning: low }
---

Drift-invariant planning notes go here.

## Final summary

Documentation and source comments made current; history confined to ADRs. SPEC-PACKAGE-LAYERING.md rewritten as a current structural reference (package layout, roles, inward-only dependency direction; migration map/anchor/today-vs-target removed). contract/README.md reworked to the per-package contract layout (concept tier, record package) with the ampersand replaced by 'and'. AGENTS.md package block lists the full current package set incl the concept tier and all generated Go targets. ARCHITECTURE.md uses record.#Artifact; the internal/deploy observed-peer hand-written rationale refreshed to current. Roadmap/planning leakage removed (DNS-RESOLVER Future-direction section with item-0017/0018, Phase tags, Makefile for-now note). CUE/Go comments dropped Phase 1/2 and later/separate-instruction pointers now that the work landed. Build wiring (embed.go, Makefile generate/specs selectors) was already current from prior arc work. Comment/doc-only; schema and golden byte-identical; make check green. Landed in a6ee10d.
