---
id: item-0001
status: done
arcs: [output-model]
rank: "0010"
title: "Wire the ADR-046 output model (one coherent instruction)"
goal: "On the ratified D9-D13 schema, the canonical one-image output model is wired end to end: imageFromStep resolves by step, the producer assembles one step image with named layers, and consumers pull only by manifest digest"
acceptance_intent: "imageFromStep resolution lands in resolveImageDigest/dag.go with ImageFromEdge shedding FromOutput; producer one-image assembly in wrapOutputs/wrapArchivedOutput (D6); consumer pull-by-digest in buildInputDelivery/producerTar/buildImageMount (D7); internal #OutputHandle (D11) introduced in artifact-api.cue and dag.go edges + run.go resolution converted to it; #ArtifactRef wire disjunction via @go(-)+glue (D12); wire output split id-less output XOR named outputs (D10); parse.go XOR enforced; a real-engine content-verifying fan-out test guards the multi-output last-write-wins defect"
links: [ADR-046, ADR-045, ADR-026, ADR-035, ADR-036, ADR-004, specs/lane.cue, specs/artifact-api.cue]
execution_profile: { class: small, reasoning: high }
---

Schema is SETTLED (D9-D12); D13 file-topology reorg landed at 8de46bd. This is the remaining wire instruction (legacy ROADMAP-ADR-046 item 3, == cross-roadmap execution-order item 7b). Settle the output model before the engine cluster. F1 (gengotypes) resolved: imageFromStep?: #Identifier @go(...,type=string) emits a bare string; presence check is != "". Follows the verified @go(-)+hand-sewn-glue pattern; do not invent a new mechanism. Byte-exact wire authored ephemerally against the then-current pin.

## Final summary

Wire the ADR-046 output model end-to-end: imageFromStep resolution, producer one-image assembly, consumer pull-by-digest, output handle wiring, and output split enforcement landed
