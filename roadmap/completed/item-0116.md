---
id: item-0116
status: done
arcs: [sigstore-harness, adr-051-sealing]
rank: "0135"
title: "Live registry-deploy integration test against a harness OCI registry"
goal: "make integration exercises the sealed registry deploy end to end against a local TLS registry: control-plane push through the anchored transport, SBOM and statement-bundle referrers attached, ObservedPeers carrying the validated identity"
acceptance_intent: "an integration test runs a lane whose deploy step pushes to a harness-local OCI registry over TLS with a declared trust anchor; the pushed subject and its five referrers are verifiable from the registry; the exported-vs-produced digest INFO line is observed with live engine data, or its absence is explained; the test runs by default and fails fast with the STRIKE_INTEGRATION=0 opt-out hint when the harness is down"
links: [test/integration, test/sigstore-local, internal/deploy/push.go, docs/ADR-051-deploy-as-sealing-point.md, item-0108]
execution_profile: { class: medium, reasoning: high }
---

item-0108 covers the sealed registry push hermetically only; make integration
has no live registry-deploy lane. This item is the end-to-end validation
item-0114 sequences itself after: a harness-local OCI registry (TLS via the
existing Caddy front, trust-anchored like the sigstore services) receives a
real lane's deploy step, and the test verifies the pushed subject plus its
five referrers (three statement bundles, two SBOMs) from the registry side.
The exported-vs-produced digest divergence (INFO-only, ADR-016
recorded-not-reacted) gets its first live engine data here -- the deferred
decision on reacting to it is taken with that data, not in this item.

## Final summary

Landed in e4f4c2c: cmd/strike/deploy_registry_integration_test.go drives a pack-then-deploy lane through the real runContext against the harness engine and zot registry over TLS, verifying the pushed subject's five referrers (two SBOM artifact types, three statement bundles annotated sealed/engine-context/informational) from the registry side. The exported-vs-produced digest divergence was observed live (they differed, consistent with ADR-016 recorded-not-reacted). mintIDToken and harness-dir resolution moved to internal/testutil, shared with the rewired TestKeylessLive.
