---
id: item-0020
status: done
arcs: [spec-layering]
rank: "0040"
title: "Rename CUE package deploy to attest and delete the re-export bridge"
goal: "specs output package is attest; the 13-alias lane bridge is gone and output refs are qualified lane definitions"
acceptance_intent: "deploy is renamed to attest with no serialized-wire or golden change; the alias block in artifact.cue is removed and every output-attestation reference resolves through a qualified lane import; generate and golden show no JSON diff"
links: [ADR-047, docs/SPEC-PACKAGE-LAYERING.md, specs/artifact.cue, specs/attestation.cue, specs/predicate.cue, specs/sigstore-bundle.cue]
execution_profile: { class: large, reasoning: high }
---

Drift-invariant planning notes go here.

## Final summary

Landed at f3ae970c. CUE package deploy renamed to attest; the 13-alias lane re-export bridge in artifact.cue deleted and output-attestation refs qualified as lane.#X; stripForConcat rewrites the lane qualifier and routes AttestationSchema through it; make specs attestation export points at ./specs:attest.
