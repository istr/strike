---
id: item-0075
status: done
arcs: [type-cleanliness]
rank: "0150"
title: "Type the trust-anchor and fingerprint fields across the contracts"
goal: "every fingerprint and authority-shaped field in the endpoint, attest, and hand-written trust surfaces carries the digest or authority type its documentation already states"
acceptance_intent: "engine and observed-peer cert fingerprints, host-key fingerprints, and the resolver record host are typed in CUE and generate typed Go; the hand-written trust mirrors carry the CUE field types; construction converges on the owning digest constructor instead of concatenation; comparisons are typed end to end; wire bytes are unchanged because the types are string-kind"
links: [docs/retrospectives/2026-07-07-dual-type-safety-audit.md, contract/endpoint/engine.cue, contract/attest/attestation.cue, internal/endpoint/trust.go]
execution_profile: { class: small, reasoning: medium }
---

Register rows R-10 through R-14. Wire-value neutral: all retypes are string-kind aliases, so serialized bytes are unchanged; the signing-payload canonicalization prerequisite landed with item-0060. The ConnectionInfo structural mirror (R-32) is deliberately out of scope; only its fingerprint fields are typed here.

## Final summary

Landed in ea6e2c5. Typed the four always-populated fingerprint fields (engine server/client cert, ObservedSSH host-key, ObservedTLS server cert) as primitive.Digest in CUE and the generated/hand-written Go mirrors and carriers across six production packages; converged the three hand-rolled sha256 concatenations and the two TLS verifiers on primitive.DigestFromHex (ADR-008); typed the peer-anchor dedup map as map[endpoint.Authority]canonicalAnchor. CABundle.Path became primitive.AbsPath (loadCABundle param retyped). Wire-value neutral: goldens unchanged, make check green. The two resolver-record fields (host, serverCertFingerprint) were descoped mid-run: sealed.resolver is required-but-emptyable, so tightening them rejects the valid empty-resolver deploy; their typing plus resolver-block optionality is deferred to item-0098 (D3 decision).
