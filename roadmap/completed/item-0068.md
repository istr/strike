---
id: item-0068
status: done
arcs: [lane-dag-decomposition]
rank: "0030"
title: "Reduce the DAG to adjacency and order, merge the walks, delete the resolved edge structs"
goal: "The DAG holds only the collapsed adjacency and the topological order, built at identifier level; the resolved edge structs and their maps are deleted; run.go consumes the lane iterators; the three predecessor walks become one walk with a collector."
acceptance_intent: "Build constructs the adjacency and Order from step reference identifiers alone with no resolved edge structs; InputEdge, PackFileEdge, DeployArtifactEdge, ImageFromEdge and the DAG edge-map fields are deleted; the DAG struct is all-unexported and drops off the coverage linter via the unexported-struct exemption, which also settles the item-0065 mixed-field concern; the run.go consumers read the lane iterators; CollectPeers, PackBaseRefs and State.CollectProvenance are unified into one walk driven by a collector; the leaf-topology validators run as the validate-dag phase; make check is green, reproducibility goldens stay byte-identical, and attestations are unchanged."
links: [internal/lane/dag.go, internal/lane/state.go, cmd/strike/run.go]
execution_profile: { class: large, reasoning: high }
---

Depends on item-0066 and item-0067. The edge maps and structs are deletable only once both the lane validation and the run consumers read the iterators. Settles item-0065 as a byproduct: an all-unexported DAG is coverage-linter exempt. Keeps the map-pair adjacency (no graph-library dependency), per code-is-liability.

## Final summary

All three instruction parts completed: DAG reduced to adjacency+order with no resolved edge structs (item-0068-1), predecessor walks unified into one iterator (item-0068-2), and validate-dag phase extracted from Build and orchestration (item-0068-3). Build constructs only adjacency and order; DAG is all-unexported; leaf-topology validators run as explicit validate-dag gate after Build. make check green, goldens and attestations unchanged.
