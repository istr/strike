---
id: item-0044
status: done
arcs: [spec-layering]
rank: "0115"
title: "Clean up residual specs/ path references after the contract/ rename"
goal: "No non-ADR doc or code references the removed specs/ path; live references point to contract/ or are dropped."
acceptance_intent: "A tree-wide search for the removed specs/ path returns only append-only ADRs and genuine historical mentions of the rename itself; the stale live references (contract/README.md, README.md, ARCHITECTURE.md, CONTRIBUTING.md, docs/CODE-STYLE.md, docs/SPEC-PACKAGE-LAYERING.md, test/crossval/README.md, and internal Go comments/strings) are corrected to contract/ paths. ADRs and the roadmap archive are out of scope."
links: [ADR-047, item-0037, item-0025, contract/README.md]
execution_profile: { class: smallest, reasoning: brief }
---

Residual fallout of the specs/ -> contract/ package rename (item-0037,
ADR-047): the directory, Makefile, and embed were updated, but prose and
comment references to the old specs/ path were left behind across docs and a
few Go files. contract/README.md is the worst offender -- its schema table
still lists specs/*.cue paths. This is a docs-and-comments hygiene pass, not a
code change.

Excluded by construction: ADRs (append-only; their specs/ mentions are correct
for their decision-time context) and the roadmap archive (historical). Genuine
narrative references to the rename itself (e.g. in SPEC-PACKAGE-LAYERING.md)
stay. The acceptance search must therefore be scoped to live references, not a
blanket no-match.

## Final summary

Residual specs/ path references from the specs/ -> contract/ rename (ADR-047) retargeted to their current contract/ per-package homes across docs (contract/README schema table, README, ARCHITECTURE, CONTRIBUTING, CODE-STYLE, ATTESTATION-SOUNDNESS, test/crossval/README) and Go comments (deploy, predicate, validate, outputref, plus two test files). The roadmap-items skill examples dropped their stale specs/*.cue link placeholders rather than rewiring them. Append-only ADRs, the roadmap item store, and the SPEC-PACKAGE-LAYERING migration narrative retain their decision-time specs/ mentions. Comment/doc-only; make check green. Landed in commit 4161f1d.
