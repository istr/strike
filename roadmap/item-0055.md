---
id: item-0055
status: ratified
arcs: [cue-coverage]
rank: "0040"
title: "Extend the trust-layer conformance test to the sealed projection"
goal: "the trust-layer conformance test machine-checks the sealed V section and the SLSA external-parameters projection, closing the gap the trust-layers map documents as pinned but not yet checked, so the duplicated sealed and external-parameters field definitions cannot drift apart unnoticed"
acceptance_intent: "the conformance test that already checks the engine-dependent and informational sections is extended to assert the sealed section and its SLSA external-parameters projection against the trust-layers map; the test moves from documenting the sealed projection as unchecked to enforcing it; the duplicated field set across the sealed attestation and the external parameters is the projection the test pins; make check is green"
links: [contract/trustlayers/trust-layers.cue, internal/deploy/trustlayers_conformance_internal_test.go, contract/attest/attestation.cue, contract/attest/predicate.cue]
execution_profile: { class: medium, reasoning: high }
---

This is the unbuilt half of the trust-layer conformance scaffold: the engine-dependent and informational sections are already checked, while the sealed section and the external-parameters projection are pinned in the map and marked not yet machine-checked (Stage-2). The test asserts field-name-set correspondence between the map and the schema sections, not field types, so it is invariant under the observed-peer and resolver retyping (item-0054, item-0049) -- there is no hard ordering dependency on them. It makes the duplicated sealed and external-parameters definitions self-checking while they remain two separate definitions.
