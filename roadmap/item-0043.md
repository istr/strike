---
id: item-0043
status: ratified
arcs: [endpoint-model]
rank: "0040"
title: "Remove the orphaned packed-authority host type after all endpoints carry Address"
goal: "once peers, the service-client endpoint, and the resolver all carry endpoint Address, the lane packed-authority host type and any residual packed-authority carriers are removed, leaving the bare-host primitive plus the boundary grammar as the only homes for the host colon port form, per ADR-048"
acceptance_intent: "a full-tree sweep confirms no remaining consumer carries the packed-authority host type; the lane Host packed host colon port definition is removed; the bare host is single-sourced in the primitive package and the packed authority exists only as a boundary parse and render over Address; no Host packed-string identity remains in the declared-endpoint contract; make check is green"
links: [ADR-048, contract/lane/transport.cue, contract/primitive]
execution_profile: { class: medium, reasoning: medium }
---

Terminal cleanup of the arc. Depends on items 0040, 0041, and 0042: the
packed-authority host type can only be removed once its last consumer has
migrated to Address. The design consideration was scoped to six CUE files,
so the executor must run a full-tree sweep at authoring time -- there may be
consumers of the packed host type beyond peers, the service endpoint, and
the resolver. Scope stays the declared axis; observed-identity types are
untouched (item-0004).
