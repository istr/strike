---
id: item-0054
status: ratified
arcs: [endpoint-model]
rank: "0050"
title: "Type the observed-peer join keys and attribution values as authorities"
goal: "the observed-peer map keys and the peer-attribution values carry the authority type that models the host-port grammar, so the attribution-to-observed-peer join runs over a single canonicalized key space rather than two free string spaces"
acceptance_intent: "the observed-peer map keys in the sealed attestation and external-parameters, and the per-step peer-attribution values that key into them, are typed as the endpoint authority constraint instead of bare host-port strings; the resolver host is out of scope, since the resolver restructure item owns it; canonicality of the join key is enforced on both sides; generated types compile, make check is green, goldens measured at execution"
links: [contract/attest/attestation.cue, contract/attest/predicate.cue, contract/endpoint/address.cue, docs/ADR-048-contract-type-semantics.md]
execution_profile: { class: small, reasoning: medium }
---

Sibling of the resolver-host typing in the dns-resolver arc (item-0049); this item covers only the observed-peer and peer-attribution host-port keys, which the contract review flagged as a join over two non-canonicalized string spaces. The authority constraint already exists and is imported by the attest package, so this is a retype, not a new type.
