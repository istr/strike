---
id: item-0120
status: ratified
arcs: [test-hygiene, sigstore-harness]
rank: "0050"
title: "Reclassify the keyless golden generator out of the default test set"
goal: "TestVerifyGoldenGenerate is an explicit fixture generator, not a default test: regenerating internal/verify/testdata/golden is a deliberate, operator-invoked action that cannot fire from a plain test run"
acceptance_intent: "a default full-tree test run, harness up or down, never rewrites the checked-in verify goldens; regeneration is a deliberate invocation documented next to the existing golden target; the generator fails loudly on harness errors instead of emitting partial fixtures; no silent env skip remains; the generator resolves the harness directory through testutil.HarnessDir instead of carrying its own copy of the path; the regeneration documentation names the fixture coupling -- golden/lane.yaml is policy input, so any lane wire change forces a full keyless regeneration, a cold-started harness re-keys trusted_root.json, and the gate is the five-file set verifying together -- and directs that a mechanically forced golden refresh lands as its own commit beside the schema change, not bundled into it"
links: [internal/deploy/verify_golden_gen_internal_test.go, internal/verify/testdata/golden, Makefile, internal/testutil/harness.go]
execution_profile: { class: small, reasoning: brief }
---

The function asserts nothing about verification; it only emits fixtures, writing internal/verify/testdata/golden with fresh nondeterministic signatures on every run. make golden does not cover it, and under the single-switch rule a default run with the harness up would silently rewrite checked-in goldens -- so it must be reclassified as a generator rather than converted to fail-fast. Its doc comment also carries an instruction reference (instruction 5a-ii) that violates the self-contained-comments rule and falls away with the rework.

Absorbed from the item-0114 completion report (two points). First, the
coupling: the verify goldens bind to the bytes of golden/lane.yaml (UC2 treats
the lane as policy), so a lane schema change cascades into a full keyless
regeneration, and known harness behavior re-keys trusted_root.json on a cold
start -- budget all five files and gate on the set verifying together. Second,
commit shape: item-0114 bundled the forced golden refresh into the schema
commit; the regeneration doc this item writes states that such a refresh is
cleanly separable and lands as its own commit for review.
