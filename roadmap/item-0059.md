---
id: item-0059
status: ratified
arcs: [cue-coverage]
rank: "0060"
title: "Investigate whether the lane.State sync mutex belongs on the serialized type"
goal: "a ratified finding on whether the lane.State runtime lock and its serialized wire fields are conflated: lane.State carries json-serialized fields that cross package boundaries (so under the rule they belong in CUE) and a sync.RWMutex that is pure behavior (so it stays hand-written), and the open question is whether the mutex belongs on the serialized type at all"
acceptance_intent: "an operator-ratified finding on whether to split the serialized wire-state (CUE-generated) from the runtime lock (hand-written), recorded as a decision; if a split is chosen, a follow-on item applies it; this item produces the finding, not the change"
links: [docs/DEVELOPMENT.md, item-0050, internal/lane/state.go]
execution_profile: { class: small, reasoning: high }
---

Surfaced during the 0050 audit and flagged by the operator as a follow-up. lane.State is the teaching case for the ratified rule: its serialized fields cross package boundaries (-> a generated state type in CUE) but the sync.RWMutex is behavior (-> stays in state.go). docs/DEVELOPMENT.md documents the mutex on lane.State. The open question is the conflation: should a serialized wire type carry a runtime lock at all, or are those two concerns on one struct? Resolve before the lane cluster migrates.
