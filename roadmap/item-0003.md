---
id: item-0003
status: proposed
arcs: [adr-038-ssh-front]
rank: "0020"
title: "ADR-038 item 8: rehost DoT resolver and TLS mediator onto the front (DNS centralization)"
goal: "The DoT resolver and TLS mediator lift from per-step capsule listeners to the single front endpoint, with DNS centralized on the same front surface"
acceptance_intent: "HTTPS is demuxed by SNI at the front; DoT resolves lane-permissively (no per-step token for DNS); resolver and TLS mediator no longer run as per-step capsule listeners; DNS is centralized on the front surface"
links: [ADR-038]
execution_profile: { class: large, reasoning: high }
---

Independent of the engine trust flip but shares the front surface, so can run in parallel under a separate operator. Last substantive ADR-038 arc; high effort. == cross-roadmap execution-order step 11. DNS centralization migrated from the retired cue-spec-review.
