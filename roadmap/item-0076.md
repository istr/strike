---
id: item-0076
status: proposed
arcs: [type-cleanliness]
rank: "0160"
title: "lane contract typing cluster: keys, references, defaults"
goal: "the lane contract stops declaring identifier-keyed maps, identifier references, path-keyed maps, and duration defaults as bare strings where the typed form is proven expressible in this repository"
acceptance_intent: "secrets and artifacts maps generate identifier-typed Go keys via the annotation form the attest package proves; the secret reference name is identifier-typed; configFiles keys carry the absolute-path primitive; the lane default timeout generates the duration type despite the literal default; the registry constraint is a named primitive instead of an inline regex; the strategy default is not re-derived in Go; the wrap-unwrap churn on the deploy-artifact path disappears with the typed keys"
links: [docs/retrospectives/2026-07-07-dual-type-safety-audit.md, contract/lane/lane.cue]
execution_profile: { class: small, reasoning: medium }
---

Register rows R-05, R-06, R-18, R-19 (contract side), R-22, R-33, R-44. The typed-key annotation form is proven by the attest package maps. The issuer and identity fields join once the OIDC typing decision (fork C) is ratified. Every CUE change in here is individually confirmation-gated per AGENTS.md.
