---
id: item-0016
status: proposed
arcs: [output-model]
rank: "0020"
title: "Machine-enforced layer-1/layer-2 direction (artifact-api boundary)"
goal: "The layer-1 (wire) vs layer-2 (internal typed API) boundary is machine-enforced, not organized by file alone: the wire may inherit the internal API but the internal API may never depend on the wire, and a violation of that direction fails CI rather than relying on review."
acceptance_intent: "The direction the file split established -- wire may inherit the internal API, never the reverse -- is enforced by tooling instead of convention: a dedicated package boundary the architecture linter checks, or a CUE-level lint, such that an internal-API-to-wire dependency fails CI; the mechanism choice is settled with the operator before authoring."
links: [ADR-046, ADR-004, specs/artifact-api.cue]
execution_profile: { class: small, reasoning: medium }
---

Deferred follow-on from ADR-046 D9 (machine-enforced direction -- a separate package or a CUE lint -- is deferred; the file boundary is the first step). Not itemized in the legacy roadmap's implementation list -- surfaced during the roadmap-store coverage review. The file boundary (artifact-api.cue, D13) landed at 8de46bd; this item is the enforcement layer on top of it. Depends on item-0001 wiring the internal API end to end. Mechanism is an open design fork (architecture-linter rule vs separate package vs CUE lint) for operator ratification before any instruction is authored. Unscheduled.
