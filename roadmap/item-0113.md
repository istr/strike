---
id: item-0113
status: proposed
arcs: [adr-051-sealing]
rank: "0130"
title: "Remove the deploy target identity (#Deploy) and #DeploySpec.target"
goal: "the deploy attestation carries no operator-declared target identity: contract/target (#Deploy), #DeploySpec.target, the internal Sealed.target, and the published ExternalParameters.target are removed, because the soundly attestable dialed endpoint is already carried by the observed-peer machinery (ADR-051 D10)"
acceptance_intent: "contract/target/target.cue and #Deploy no longer exist; #DeploySpec.target, attest Sealed.target and StrikeExternalParameters.target, and the trust-layers target entry are removed with their imports; no Go consumer references target.Deploy (deploy.go, project.go, and all test fixtures updated); golden attestation fixtures regenerate without the target field; make check and make golden are green"
links: [docs/ADR-051-deploy-as-sealing-point.md, contract/target/target.cue, contract/lane/lane.cue, contract/attest/attestation.cue, contract/attest/predicate.cue, contract/trustlayers/trust-layers.cue, internal/deploy/deploy.go]
execution_profile: { class: small, reasoning: low }
---

Applies ADR-051 D10. The deploy target identity had reduced to a self-asserted
operator label (target.id) with no verifiable value: strike performs only
Recording (ADR-016), and an offline verifier can authenticate the signed label
but cannot confirm it denotes a real destination. The soundly attestable dialed
endpoint is already carried in Sealed.ObservedPeers (declared-to-dialed-to-
observed), and the artifact subject (ADR-051 D2) covers the payload; no strike
code read any identity field.

This removal is the resolution of the deploy-target naming, type, and namespace
questions (originating item-0057): with #DeploySpec.target gone there is no name
clash with the registry method's push destination, no target type beside the
method enum, and no target namespace beside the kubernetes method namespace. The
kubernetes dialed-target representation is the observed cluster peer and is
unaffected; any refinement belongs to the kubernetes rewiring (cf. ADR-051 D9),
not this item.

Blast radius: contract/target/target.cue (deleted), contract/lane/lane.cue,
contract/attest/attestation.cue, contract/attest/predicate.cue,
contract/trustlayers/trust-layers.cue; internal/deploy/deploy.go and
internal/deploy/project.go; roughly 26 target.Deploy{...} fixture lines across
seven _test.go files; and golden attestation regeneration.
