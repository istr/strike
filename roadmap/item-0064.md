---
id: item-0064
status: ratified
arcs: [beta-readiness]
rank: "0030"
title: "Repair markdown cross-reference rot and gate relative doc links"
goal: "the dead relative markdown references are repaired in one documentation-only change and a lint gate over relative markdown links keeps the class from recurring"
acceptance_intent: "a link gate over relative markdown references is wired into make lint and passes on the repaired tree; the repair covers the ADR cross-links still pointing at pre-rename ADR filenames, the SECURITY.md docs/adr path, the transient PR-18 reference, and the stale README project-structure section, including the wrong generated filename (cue_types_lane_gen.go is now lane.gen.go plus lane_enum.gen.go) and the CLI entry-point line that omits the verify and deploy subcommands; ADR-body link repairs follow the append-only policy in whichever mode the operator ratifies"
links: [SECURITY.md, README.md, docs/ADR-028-step-container-egress-mediation.md, docs/ADR-033-ssh-peer-egress-and-unified-mediation.md, docs/ADR-037-two-engine-trust-layers.md, item-0051]
execution_profile: { class: small, reasoning: none }
---

Found at 3db7843: nine dead relative links across ADR-028, ADR-033, ADR-037
and SECURITY.md, all pointing at ADR filenames from before the rename wave
(per-step-security-profile, typed-secrets, audit-pipeline, the old ssh names)
plus one docs/adr/ path that never existed; the README project-structure
section lists five of twenty-three internal packages and a single lane.cue.
Repair-mode fork for the operator: in-place mechanical link-target fixes
inside ADR bodies versus appended errata, per the append-only policy. Distinct
from item-0051 (embed-claim prose reconciliation), which stays its own item;
the two bundle naturally into one docs-only PR under the risk-clustering rule.
Optional candidate, operator decides: a non-systemd podman-socket note in the
README prerequisites. The ratified beta deferrals of item-0003 and item-0015
must be stated as limitations in the shipped docs before the beta tag; that
one-sentence-each note can ride this PR or its own.
