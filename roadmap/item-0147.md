---
id: item-0147
status: proposed
arcs: [parked, test-hygiene]
rank: "0150"
title: "Revive or delete the five parked lane fixtures"
goal: "the five .yaml.parked lane fixtures are either parseable against the current schema and unparked, or deleted, so no file in the tree is a frozen sample of a format that no longer exists"
acceptance_intent: "each of the five parked fixtures is resolved individually rather than as a batch: cmd/strike/testdata/hugo_like_lane.yaml.parked, fan_out_lane.yaml.parked, image_from_lane.yaml.parked, internal/lane/testdata/valid_force_run.yaml.parked, and valid_deploy.yaml.parked; a revived fixture parses and validates under the current lane schema and is wired into the test that motivated it, and a deleted one leaves no dangling reference; the parked note that names ADR-051 D6 and D9 as the reason is either discharged or restated against what still blocks the fixture"
links: [ADR-051, item-0049, cmd/strike/testdata, internal/lane/testdata]
execution_profile: { class: small, reasoning: brief }
---

Surfaced by the item-0049 landing run, which rewrote every lane fixture's
resolver block and could not see these five: they carry a .parked extension, so
the *.yaml discovery grep skipped them and no test parses them.

They are not merely stale in the resolver field. valid_deploy.yaml.parked also
carries a top-level registry: key that #Lane does not declare, and its own
header says it is invalid under ADR-051 D6 and D9. So a fixture that has drifted
through at least one ADR cycle while parked is not saved work; whoever revives
it rewrites it anyway. Patching only the resolver block would leave them
unparseable while making them look maintained, which is why item-0049
deliberately left them alone.

Leaning toward deletion under "removal is the default resolution", but the
parked notes claim reactivation is intended, so the call is the operator's and
is made per file, not in bulk.
