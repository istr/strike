---
id: item-0080
status: ratified
arcs: [type-cleanliness]
rank: "0200"
title: "Typed capsule identity, canonical host, and the twin collapse"
goal: "the capsule subsystem carries a ratified typed unit identity and a ratified canonical-host type end to end, and the byte-identical helper twins that couple packages by comment disappear"
acceptance_intent: "capsule, resolver, mediator, and the ssh forwarder take the ratified unit key and port allocation and records key on it; ssh target host and port and the recorded connection identities are primitive-typed with the known-hosts render-and-reparse round trip reduced to the single parse boundary; peer maps, allowlists, and the certificate cache key on the canonical-host type; capture snapshot fields are typed; the duplicated canonicalize, decision, timeout, capture-key, and ssh-target helpers have single owners; the three structurally identical upstream-lookup function types declared in capsule and mediator collapse to one owner and take the canonical-host type instead of a plain string; once an incoming SNI has matched the peer allowlist, the name the mediator resolves upstream, records as the observed peer address, and sends as the upstream SNI is taken from the matched lane declaration rather than from the container-supplied SNI string, a peer-map hit proving the two byte-identical after canonicalization so that nothing on the wire changes; the peer map being keyed on the SNI name alone, so that two peers declared on the same host with different ports collide at construction, is either documented as the intended consequence of SNI dispatch or rejected with a named error; the typeconv allowlist deferral is retired with this item as its owner"
links: [docs/retrospectives/2026-07-07-dual-type-safety-audit.md, internal/capsule/capsule.go, internal/mediator/mediator.go, internal/resolver/resolver.go, internal/capsule/sshforward.go]
execution_profile: { class: medium, reasoning: deep }
---

Register rows R-01, R-08, R-09, R-11, R-17, R-30, R-31. Forks A and B are ratified (both option 1). Fork A: the unit key is a comparable value struct CapsuleUnit{Step primitive.Identifier; Capture primitive.Identifier}, empty Capture meaning "the step itself", used directly as the comparable map key, as the constructor parameter, and rendered for display/logs by one owned method; both captureKey twins and the "capture:<step>:<capture>" string protocol are removed and stepPorts becomes map[CapsuleUnit]HostPorts. It is accepted as in-process identity / disjunction glue, a hand-written comparable struct, not a CUE schema gap. Fork B: the canonical host is endpoint.CanonicalHost (hand-written, concept tier) with the single constructor Canonicalize(primitive.Host) (CanonicalHost, error); the mediator peer map, resolver allowlist, and ephemeral-CA cache key on it, while QueryRecord.QName stays string by design (untrusted wire name from container DNS packets); the linttypeconv M3 allowlist deferral is retired with this item as its owner. This is the highest-judgment item of the program; the capture-composite domain fact (retrospective 4.1.1) is the reason the unit key is a ratified design, not an executor discovery.

Amended after a whole-strand read of mediator, peer and dial. Two additions,
both inside this item's existing charter rather than new work. First, the
upstream-lookup closure is declared as three structurally identical named
function types (capsule.UpstreamLookupFunc, mediator.UpstreamLookupFunc, and a
third named in a mediator comment that does not exist), converted between at the
capsule-to-mediator seam, and carrying the name as a plain string where the
canonical-host type applies; that is the same duplication class as the
canonicalize and ssh-target twins already listed. Second, Fork B keys the peer
map on the canonical host, which makes two peers declared on the same host with
different ports collide as a duplicate; the collision follows from SNI dispatch
having no port to dispatch on, and needs to be a stated consequence rather than
a surprise at construction time.

Third addition from the same read, and the reason Fork B's provenance rule
needs restating on the TLS side: after the peer-map lookup succeeds, the
mediator still resolves and records the container-supplied SNI string rather
than the declared host it just matched. The two are equal by construction
today, since the map key is the canonicalized declared host and the lookup hit
proves the canonicalized forms match, so this is a provenance inversion rather
than a hole -- but the DoT query and the observed-peer key both derive from
container input while the lane's own typed value sits one field away. The SNI
sent upstream is not a separate matter: it is the same name, taken from the
same declaration. A peer-map hit makes the declared canonical host and the
canonicalized container SNI byte-identical, so this is about which value is
program state and not about what reaches the wire. It is the same distinction
Fork B already draws when it keeps QueryRecord.QName a plain string.

Amended 2026-08-31. The ADR attribution the paragraph above used to carry was
invented: neither ADR-028 nor ADR-030 nor ADR-033 states an SNI-preservation
rule, and ADR-028's "Component 2: Controller-side mediation" describes the
mediator terminating container-side TLS and re-originating upstream to the
declared peer. Its only SNI passage argues that the value is what the container
claimed and may be invisible under ECH, which is that passage's case for
terminating rather than forwarding. The same false attribution had reached
source, in internal/mediator's package doc and in dialUpstream's comment, and
was corrected in all three places in one pass.

Two clauses of the acceptance intent are delivered by item-0130 instruction 1
and are not this item's work any more. The TLS-side provenance rule is met: the
mediator resolves, verifies, sends as SNI and records the matched peer's
declared canonical host, carried on peerEntry.name, and the recorded peer
authority is unchanged. The three upstream-lookup function types are gone,
collapsed onto one owner, a *transport.Dialer value that both capsule and
mediator hold. What remains of that clause here is the type: Dialer.LookupHost
still takes a plain string, so Fork B's CanonicalHost has yet to reach it, and
peerEntry.name anticipates it as a primitive.Host.
