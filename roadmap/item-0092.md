---
id: item-0092
status: ratified
arcs: [type-cleanliness]
rank: "0205"
title: "Collapse the hand-written ConnectionInfo mirror of the engine union"
goal: "the observed engine connection identity exists once, as the generated union, instead of once generated and once hand-written"
acceptance_intent: "container.ConnectionInfo no longer restates the fields of the endpoint engine union; the engine identity the container package captures is carried in the contract-generated type; the deploy switch that rebuilds the union from the mirror is gone; sealed.engine content is unchanged"
links: [docs/retrospectives/2026-07-07-dual-type-safety-audit.md, docs/ADR-049-type-discipline-internal-seams.md, internal/container/engine.go, internal/deploy/deploy.go, contract/endpoint/engine.cue]
execution_profile: { class: small, reasoning: medium }
---

Register row R-32, recorded as an open candidate by the dual audit and given an owner here. The mirror is why the wire-discriminator literals appear in a producer (container) and a consumer (deploy) rather than only at the owning seam. Depends on the enum constant visibility decision.
