---
id: item-0041
status: ratified
arcs: [endpoint-model]
rank: "0020"
title: "Merge the HTTPS service-client endpoint onto TLSEndpoint and drop the url string"
goal: "the ADR-040 service-client HTTPS endpoint becomes an endpoint TLSEndpoint (address plus TLS trust anchor); the url-string identity field is removed and the https base form is projected at the boundary over an address whose path carries the base path, per ADR-048"
acceptance_intent: "HTTPSEndpoint as a url-keyed type is gone; service clients that dial fixed well-known paths declare an endpoint TLSEndpoint whose address path carries the base path and whose trust anchor is Trust; the https-only url grammar survives only as a boundary constraint and projection, not as the stored identity; the ADR-040 keyless service clients compile against the concept type; make check is green"
links: [ADR-048, ADR-040, contract/endpoint/address.cue, contract/lane/transport.cue]
execution_profile: { class: large, reasoning: deep }
---

Depends on item-0040 (the TLSEndpoint shape must exist). One of three
TLS-family endpoints that collapse onto the same Address-plus-Trust shape;
among them the protocol does not distinguish, the lane-side role does, which
is why they merge to one concept type. The url:"^https://" identity field is
the representation leak ADR-048 fact 1 forbids; it becomes a boundary
constraint and projection, with the base path carried by Address.path.
Touches the ADR-040 keyless service-client path.
