---
id: item-0160
status: ratified
arcs: [beta-readiness]
rank: "0035"
title: "Reproduce the released strike binary and image outside the bootstrap: commit gate and strike-free rebuild"
goal: "the released image digest and binary hash are reproduced by a control plane other than the one that produced them, on an engine on another host -- the commit gate running the released image as a sibling on the forge runner -- and the binary is reproduced without strike by a plain go build in the pinned Go image, so the ADR-009 proof no longer rests on one control plane, one engine and one host"
acceptance_intent: "on a release commit the commit gate packs the binary its build step produces with the bootstrap lane's pack spec, and the resulting digest equals the digest the bootstrap lane published for that commit, checked by the release procedure from the second release on and recorded with the release; the release procedure documents a podman run of the pinned Go image that rebuilds the binary from the tagged commit with the lane's flags, its sha256 equals the binary in the released image, and that command needs a rootless engine and nothing else (ADR-055 D1); the stage_1 binary the Containerfile builds is compared against the lane-built binary as an informational check first and promoted to a gate once the toolchain versions and flags match; a mismatch in a gating check blocks the tag"
depends_on: [item-0062, item-0063, item-0155]
links: [item-0062, item-0063, item-0150, docs/ADR-009-bootstrap-reproducibility-proof.md, docs/ADR-055-hardened-endpoint-development-premise.md, README.md, bootstrap/Containerfile, lane.yaml]
execution_profile: { class: small, reasoning: medium }
---

The two checks are the cross-control-plane and the strike-free halves of the
reproduction matrix. The commit gate's build step is already a plain go build
in a digest-pinned Go image orchestrated by the released strike as a sibling
(item-0063), so the cross-control-plane half costs one pack step and one
recorded digest, not a second pipeline. With one topology in the tree, the
engine diversity of this check comes from the two hosts -- the operator's
endpoint and the forge runner -- not from a nested engine; running both
halves on one machine proves less, and the release procedure says so. The
strike-free half replaces ADR-009's current advice that a distrusting
operator run the bootstrap locally, which still requires trusting strike.
The stage_1-versus-lane binary comparison is a bonus: the Containerfile
builds with the go.dev toolchain of the golang alpine image and the lane
with a Wolfi-built toolchain, so equality at matching versions is agreement
between two independently built compilers of the same version, the cheapest
form of toolchain diversity available; it stays informational until
item-0062 aligns flags and versions, and it is lost if item-0062 moves the
builder stage onto the same toolchain image the lane uses -- that trade is
named there.
